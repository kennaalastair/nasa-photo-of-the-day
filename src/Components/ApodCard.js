import React from "react";

export default function ApodCard({ imgUrl }) {
    return (
        <div className="apod-card">
            {/* <img src={imgUrl} style={{ maxWidth: "175px" }} /> */}
        </div>
    );
}